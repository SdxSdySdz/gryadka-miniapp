# 🚀 Деплой проекта "Грядка"

## 📊 Архитектура деплоя (минимум хостингов)

```
┌─────────────────────────────────────────────┐
│  GitHub Pages (БЕСПЛАТНО)                   │
│  https://username.github.io/gryadka/        │
│                                             │
│  📱 Mini App (HTML/CSS/JS)                  │
└──────────────────┬──────────────────────────┘
                   │
                   │ fetch()
                   ↓
┌─────────────────────────────────────────────┐
│  Render.com (БЕСПЛАТНО)                     │
│  https://gryadka-api.onrender.com           │
│                                             │
│  🤖 Telegram Bot (Python)                   │
│  🌐 API (FastAPI)                           │
│  🗄️  База данных (SQLite)                   │
└─────────────────────────────────────────────┘
```

**Итого: 2 бесплатных хостинга**

---

## 🎯 План деплоя

1. ✅ **GitHub Pages** → Mini App (фронтенд)
2. ✅ **Render.com** → Bot + API (бэкенд)

---

## 📦 Часть 1: Деплой Mini App на GitHub Pages

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Назовите: `gryadka-miniapp`
4. Сделайте **Public** (для бесплатного GitHub Pages)
5. Нажмите "Create repository"

### Шаг 2: Загрузите код на GitHub

```bash
cd /Users/sdxsdysdz/Desktop/Programs/Freelance/Farid/Gryadka

# Инициализируйте Git
git init
git add .
git commit -m "Initial commit: Gryadka project"

# Подключите репозиторий (замените YOUR-USERNAME)
git remote add origin https://github.com/YOUR-USERNAME/gryadka-miniapp.git
git branch -M main
git push -u origin main
```

### Шаг 3: Включите GitHub Pages

1. Зайдите в Settings репозитория
2. Перейдите в **Pages**
3. В **Source** выберите:
   - Branch: `main`
   - Folder: `/docs`
4. Нажмите **Save**

✅ **Mini App будет доступен:**
```
https://YOUR-USERNAME.github.io/gryadka-miniapp/
```

---

## 🖥️ Часть 2: Деплой Bot + API на Render.com

### Шаг 1: Зарегистрируйтесь на Render

1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub
3. Нажмите "New" → "Web Service"

### Шаг 2: Подключите репозиторий

1. Выберите репозиторий `gryadka-miniapp`
2. Нажмите "Connect"

### Шаг 3: Настройте сервис

**Основные настройки:**
- **Name**: `gryadka-api`
- **Region**: выберите ближайший
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Runtime**: `Python 3`
- **Build Command**:
  ```bash
  pip install -r requirements.txt
  ```
- **Start Command**:
  ```bash
  python run.py
  ```

**Environment Variables** (переменные окружения):

Добавьте все переменные из вашего `.env`:

```
BOT_TOKEN=ваш_токен_бота
ADMIN_ID=587362201
DATABASE_URL=sqlite+aiosqlite:///./gryadka.db
MINI_APP_URL=https://YOUR-USERNAME.github.io/gryadka-miniapp/
API_HOST=0.0.0.0
API_PORT=8000
SECRET_KEY=ваш_секретный_ключ
```

⚠️ **Важно:** Замените `MINI_APP_URL` на реальный URL GitHub Pages!

### Шаг 4: Создайте файл для Render

Создайте файл `render.yaml` в корне проекта:

```yaml
services:
  - type: web
    name: gryadka-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
```

### Шаг 5: Деплой

1. Нажмите "Create Web Service"
2. Подождите 5-10 минут (первый деплой)
3. Render автоматически установит зависимости и запустит приложение

✅ **API будет доступен:**
```
https://gryadka-api.onrender.com
```

---

## ⚙️ Часть 3: Финальная настройка

### 1. Обновите API URL в Mini App

Откройте `docs/config.js` и измените:

```javascript
const CONFIG = {
    API_BASE_URL: 'https://gryadka-api.onrender.com', // Ваш реальный URL
    VERSION: '1.0.0'
};
```

Сохраните и запушьте:
```bash
git add docs/config.js
git commit -m "Update API URL"
git push
```

### 2. Настройте CORS в API

Откройте `api/main.py` и добавьте CORS middleware:

```python
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="Грядка API")

# Добавьте CORS
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://YOUR-USERNAME.github.io",  # GitHub Pages
        "http://localhost:8001",            # Локальная разработка
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

Замените `YOUR-USERNAME` на ваш GitHub username!

Сохраните и запушьте:
```bash
git add api/main.py
git commit -m "Add CORS for GitHub Pages"
git push
```

### 3. Обновите бота с URL Mini App

В `.env` на Render укажите правильный `MINI_APP_URL`:
```
MINI_APP_URL=https://YOUR-USERNAME.github.io/gryadka-miniapp/
```

Render автоматически перезапустит приложение после изменения переменных.

---

## 🎉 Готово!

### Проверьте работу:

1. **Mini App**: https://YOUR-USERNAME.github.io/gryadka-miniapp/
2. **API**: https://gryadka-api.onrender.com/docs
3. **Telegram Bot**: Найдите бота в Telegram, отправьте `/start`

---

## 📝 Важные замечания

### ⚠️ Render Free Tier ограничения:

- ✅ 750 часов/месяц бесплатно
- ⚠️ Сервис "засыпает" после 15 минут неактивности
- ⏱️ Первый запрос после "сна" займет ~30 секунд
- 💡 Решение: используйте UptimeRobot для пинга каждые 5 минут

### ⚠️ GitHub Pages ограничения:

- ✅ 100GB трафика/месяц
- ✅ 1GB размер сайта
- ✅ 10 билдов/час
- 💡 Этого более чем достаточно для Mini App

---

## 🔄 Обновление проекта

### Обновить Mini App:
```bash
git add docs/
git commit -m "Update Mini App"
git push
```
GitHub Pages обновится через 1-2 минуты.

### Обновить Bot/API:
```bash
git add .
git commit -m "Update API"
git push
```
Render автоматически задеплоит новую версию за 3-5 минут.

---

## 🆘 Решение проблем

### Mini App не загружается
- ✅ Проверьте GitHub Pages настройки
- ✅ Убедитесь, что `/docs` выбрана как источник
- ✅ Проверьте консоль браузера (F12)

### API не отвечает
- ✅ Проверьте логи на Render
- ✅ Убедитесь, что переменные окружения установлены
- ✅ Проверьте, что сервис запущен

### CORS ошибки
- ✅ Добавьте GitHub Pages URL в CORS origins
- ✅ Проверьте, что middleware добавлен в `api/main.py`

### Бот не открывает Mini App
- ✅ Проверьте `MINI_APP_URL` в переменных Render
- ✅ Убедитесь, что URL правильный (с https://)
- ✅ Перезапустите бота на Render

---

## 💰 Стоимость

- **GitHub Pages**: **$0/месяц** (бесплатно навсегда)
- **Render Free Tier**: **$0/месяц** (750 часов бесплатно)

**Итого: $0/месяц** 🎉

---

## 🚀 Альтернативные хостинги (если нужно)

Если Render не подходит:

1. **Railway.app** - $5 бесплатных кредитов/месяц
2. **Fly.io** - 3 VM бесплатно
3. **Heroku** - платный ($7/месяц)
4. **VPS (DigitalOcean)** - $4-6/месяц

---

## ✅ Финальный чек-лист

- [ ] Репозиторий создан на GitHub
- [ ] GitHub Pages настроен на `/docs`
- [ ] Mini App открывается по URL
- [ ] Render сервис создан
- [ ] Переменные окружения установлены
- [ ] `config.js` обновлен с API URL
- [ ] CORS настроен в API
- [ ] Бот открывает Mini App
- [ ] Админка работает
- [ ] Можно добавлять товары

---

Удачи с деплоем! 🚀
