# 🚀 Быстрый старт - Грядка

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Получите токен бота от [@BotFather](https://t.me/botfather)

3. Узнайте свой Telegram ID от [@userinfobot](https://t.me/userinfobot)

4. Отредактируйте `.env`:
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # Ваш токен бота
ADMIN_ID=123456789  # Ваш Telegram ID
MINI_APP_URL=https://your-domain.com  # URL вашего Mini App
```

## Шаг 3: Инициализация базы данных

```bash
python init_db.py
```

Этот скрипт создаст:
- ✅ Таблицы базы данных
- ✅ Тестовые категории и товары
- ✅ Настройки по умолчанию
- ✅ Интервалы доставки
- ✅ Тестовый промокод WELCOME

## Шаг 4: Запуск приложения

### Вариант A: Запуск всего одной командой
```bash
python run.py
```

### Вариант B: Запуск компонентов отдельно

**Терминал 1 - Telegram бот:**
```bash
python bot/main.py
```

**Терминал 2 - API сервер:**
```bash
python api/main.py
```

**Терминал 3 - Mini App:**
```bash
python -m uvicorn mini_app.app:app --host 0.0.0.0 --port 8001 --reload
```

## Шаг 5: Проверка работы

1. **Telegram бот:**
   - Найдите своего бота в Telegram
   - Отправьте команду `/start`
   - Должно появиться приветственное сообщение

2. **API:**
   - Откройте http://localhost:8000/docs
   - Должна открыться документация Swagger

3. **Mini App:**
   - Откройте http://localhost:8001
   - Должна загрузиться главная страница магазина

## 🎯 Тестовые данные

После инициализации БД доступны:

**Категории:**
- Все
- Ягоды
- Цитрусовые
- Яблоки
- Овощи

**Товары:**
- Клубника (Хит)
- Апельсины (Акция)
- Яблоки Гала
- Помидоры (Советую)

**Промокод:**
- Код: `WELCOME`
- Скидка: 10%
- Минимальный заказ: 1000 ₽

## 🔧 Настройка Mini App в Telegram

1. Отправьте команду [@BotFather](https://t.me/botfather): `/newapp`
2. Выберите вашего бота
3. Введите название приложения: `Грядка`
4. Введите описание
5. Загрузите иконку (512x512 px)
6. Отправьте URL: `https://your-domain.com`
7. Загрузите скриншот
8. Подтвердите создание

## 🌐 Деплой на сервер

### Использование nginx + systemd

**1. Установите nginx:**
```bash
sudo apt install nginx
```

**2. Создайте systemd сервисы:**

`/etc/systemd/system/gryadka-bot.service`:
```ini
[Unit]
Description=Gryadka Telegram Bot
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/Gryadka
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python bot/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

**3. Запустите сервисы:**
```bash
sudo systemctl enable gryadka-bot
sudo systemctl start gryadka-bot
```

## 📱 Локальное тестирование Mini App

Для локального тестирования используйте ngrok:

```bash
# Установите ngrok
npm install -g ngrok

# Запустите туннель
ngrok http 8001

# Скопируйте HTTPS URL и обновите MINI_APP_URL в .env
```

## ❗ Частые проблемы

### Ошибка: "Bot token is invalid"
- Проверьте правильность токена в `.env`
- Убедитесь, что токен скопирован полностью

### Ошибка: "Access denied"
- Проверьте, что ваш Telegram ID указан в ADMIN_ID
- ID должен быть числом без кавычек

### База данных не создается
- Проверьте права доступа к директории
- Убедитесь, что установлен SQLite

### Mini App не открывается в Telegram
- Проверьте, что URL доступен извне (используйте ngrok для локального тестирования)
- URL должен использовать HTTPS

## 📞 Поддержка

Если проблема не решена:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Python (должна быть 3.11+)

## ✅ Чеклист запуска

- [ ] Установлены зависимости
- [ ] Создан файл .env
- [ ] Получен токен бота
- [ ] Узнан Telegram ID администратора
- [ ] Инициализирована база данных
- [ ] Запущен бот
- [ ] Запущен API сервер
- [ ] Запущен Mini App
- [ ] Бот отвечает на /start
- [ ] API Docs открываются
- [ ] Mini App загружается

Готово! 🎉
