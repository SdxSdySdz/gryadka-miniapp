# 🚀 Быстрая инструкция: Деплой на GitHub Pages

## ✅ Что уже готово:

- ✅ Папка `docs/` создана с статической версией Mini App
- ✅ `config.js` настроен для автоматического определения API URL
- ✅ Все JavaScript файлы обновлены для использования CONFIG
- ✅ HTML файлы адаптированы с относительными путями
- ✅ CORS настроен в API
- ✅ `.gitignore` создан
- ✅ `render.yaml` готов для деплоя на Render

---

## 📋 Шаги для деплоя (5 минут)

### Шаг 1: Создайте репозиторий на GitHub (2 мин)

1. Перейдите на https://github.com/new
2. Название: `gryadka-miniapp` (или любое)
3. Сделайте **Public** ✅
4. НЕ добавляйте README/License (уже есть)
5. Нажмите **"Create repository"**

### Шаг 2: Загрузите код (1 мин)

Скопируйте и выполните команды (замените `YOUR-USERNAME` на ваш GitHub username):

```bash
cd /Users/sdxsdysdz/Desktop/Programs/Freelance/Farid/Gryadka

git init
git add .
git commit -m "Initial commit: Gryadka Mini App"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/gryadka-miniapp.git
git push -u origin main
```

### Шаг 3: Включите GitHub Pages (1 мин)

1. Зайдите в **Settings** репозитория
2. Слева выберите **Pages**
3. В **Source**:
   - Branch: **`main`**
   - Folder: **`/docs`** ⚠️ Важно!
4. Нажмите **Save**
5. Подождите 1-2 минуты

✅ **Готово!** Ваш сайт: `https://YOUR-USERNAME.github.io/gryadka-miniapp/`

### Шаг 4: Обновите API URL (1 мин)

Откройте `docs/config.js` и измените URL (после деплоя API на Render):

```javascript
const CONFIG = {
    API_BASE_URL: 'https://gryadka-api.onrender.com', // Ваш реальный URL
    VERSION: '1.0.0'
};
```

Сохраните и запушьте:
```bash
git add docs/config.js
git commit -m "Update API URL"
git push
```

---

## 🎯 Следующий шаг: Деплой API на Render

См. подробную инструкцию в [DEPLOYMENT.md](DEPLOYMENT.md)

Краткая версия:

1. Зарегистрируйтесь на https://render.com
2. Создайте **New Web Service**
3. Подключите репозиторий
4. Настройте переменные окружения (BOT_TOKEN, ADMIN_ID, MINI_APP_URL)
5. Деплой!

После деплоя API обновите `docs/config.js` с реальным URL.

---

## ✅ Финальный чек-лист

- [ ] Репозиторий создан на GitHub
- [ ] Код загружен (`git push`)
- [ ] GitHub Pages включен (Settings → Pages → `/docs`)
- [ ] Mini App открывается по URL
- [ ] API задеплоен на Render
- [ ] `config.js` обновлен с правильным API URL
- [ ] `MINI_APP_URL` обновлен в переменных Render
- [ ] Бот открывает Mini App
- [ ] Админка работает

---

## 🌐 Ваши URL после деплоя

- **Mini App**: `https://YOUR-USERNAME.github.io/gryadka-miniapp/`
- **API**: `https://gryadka-api.onrender.com`
- **API Docs**: `https://gryadka-api.onrender.com/docs`

---

## 📝 Важные замечания

1. **GitHub Pages бесплатен** для публичных репозиториев
2. **Render бесплатен** на Free tier (750 часов/месяц)
3. **Сервис на Render "засыпает"** после 15 минут - первый запрос будет медленным
4. **CORS уже настроен** в API для работы с любым доменом
5. **База данных SQLite** будет пересоздаваться при перезапуске Render (используйте PostgreSQL для продакшена)

---

## 🆘 Если что-то не работает

### Mini App не открывается
```bash
# Проверьте, что папка /docs выбрана в GitHub Pages
# Settings → Pages → Source → /docs
```

### API не отвечает
```bash
# Проверьте логи на Render
# Dashboard → Service → Logs
```

### CORS ошибка
```bash
# Проверьте, что CORS middleware добавлен в api/main.py
# Уже добавлен! Должно работать.
```

---

## 🎉 Готово!

После выполнения этих шагов ваш Mini App будет доступен всему миру через GitHub Pages, а API будет работать на Render!

**Итого стоимость: $0/месяц** 💰

Удачи! 🚀
