# 🌐 Грядка Mini App - Статическая версия для GitHub Pages

Это статическая версия Mini App для деплоя на GitHub Pages.

---

## 📋 Что здесь находится?

```
docs/
├── index.html              # Главная страница магазина
├── config.js               # Конфигурация API URL
├── admin/
│   ├── index.html          # Главная админ-панели
│   └── products.html       # Управление товарами
└── static/
    ├── css/                # Стили
    ├── js/                 # JavaScript
    └── images/             # Изображения
```

---

## 🚀 Деплой на GitHub Pages

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий, например: `gryadka-miniapp`
4. Сделайте его **Public** (для бесплатного GitHub Pages)
5. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
6. Нажмите "Create repository"

### Шаг 2: Инициализируйте Git в проекте

Откройте терминал в корне проекта:

```bash
# Инициализируйте Git (если еще не сделано)
cd /Users/sdxsdysdz/Desktop/Programs/Freelance/Farid/Gryadka
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Gryadka Mini App"

# Подключите удаленный репозиторий (замените YOUR-USERNAME и YOUR-REPO)
git remote add origin https://github.com/YOUR-USERNAME/YOUR-REPO.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

### Шаг 3: Настройте GitHub Pages

1. Перейдите в Settings репозитория на GitHub
2. В боковом меню выберите **Pages**
3. В разделе **Source** выберите:
   - **Branch**: `main`
   - **Folder**: `/docs`
4. Нажмите **Save**
5. Подождите 1-2 минуты

✅ **Ваш сайт будет доступен по адресу:**
```
https://YOUR-USERNAME.github.io/YOUR-REPO/
```

---

## ⚙️ Настройка API URL

После деплоя API на Render/Railway, обновите `config.js`:

```javascript
const CONFIG = {
    API_BASE_URL: 'https://gryadka-api.onrender.com', // Ваш API URL
    VERSION: '1.0.0'
};
```

**Не забудьте сделать коммит и push после изменения!**

```bash
git add docs/config.js
git commit -m "Update API URL"
git push
```

---

## 🔧 Локальное тестирование

Для локального тестирования запустите простой HTTP сервер:

```bash
cd docs
python3 -m http.server 8080
```

Откройте: http://localhost:8080

---

## 🌍 CORS настройки для API

Чтобы GitHub Pages мог обращаться к API, добавьте в API (FastAPI) CORS:

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://YOUR-USERNAME.github.io",
        "http://localhost:8080",  # Для локальной разработки
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## 📝 Обновление Mini App

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Pages автоматически обновится за 1-2 минуты.

---

## ✅ Чек-лист для запуска

- [ ] Репозиторий создан на GitHub
- [ ] Код залит в репозиторий
- [ ] GitHub Pages настроен на `/docs`
- [ ] API задеплоен на Render/Railway
- [ ] `config.js` обновлен с правильным API URL
- [ ] CORS настроен в API
- [ ] Mini App URL обновлен в `.env` бота (`MINI_APP_URL`)
- [ ] Telegram бот перезапущен

---

## 🎉 Готово!

Теперь ваш Mini App доступен по адресу:
```
https://YOUR-USERNAME.github.io/YOUR-REPO/
```

И будет работать через API на Render!

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что API URL правильный в `config.js`
3. Проверьте CORS настройки в API
4. Убедитесь, что API запущен и доступен
