# 🚀 Запуск проекта "Грядка" локально

## 📋 Предварительные требования

- **Python 3.10+** (проверить: `python3 --version`)
- **pip** (менеджер пакетов Python)
- **Git** (для клонирования репозитория)
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/BotFather))
- **Ваш Telegram ID** (получить у [@userinfobot](https://t.me/userinfobot))

---

## 🔧 Установка и настройка

### Шаг 1: Установка зависимостей

```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение

# На macOS/Linux:
source venv/bin/activate

# На Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 2: Создание .env файла

Скопируйте `.env.example` и заполните своими данными:

```bash
cp .env.example .env
```

Откройте `.env` в редакторе и заполните:

```env
# Telegram Bot Configuration
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz  # От @BotFather
ADMIN_ID=123456789  # Ваш Telegram ID от @userinfobot

# Database Configuration
DATABASE_URL=sqlite+aiosqlite:///./gryadka.db

# Mini App Configuration
MINI_APP_URL=http://localhost:8001

# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Security
SECRET_KEY=your_secret_key_here_change_this_in_production

# Payment Configuration (опционально)
PAYMENT_PROVIDER_TOKEN=
```

**Важно:** 
- `BOT_TOKEN` обязателен для работы бота
- `ADMIN_ID` — ваш Telegram ID для доступа к админ-панели

### Шаг 3: Инициализация базы данных

```bash
python init_db.py
```

Эта команда:
- Создаст файл базы данных `gryadka.db`
- Создаст все таблицы
- Добавит тестовые данные (категории, товары, настройки, FAQ)

Вы увидите:
```
🔄 Инициализация базы данных...
📦 Создание категорий...
🍎 Создание товаров...
⚙️  Создание настроек...
✅ База данных успешно инициализирована!
```

---

## ▶️ Запуск приложения

### Вариант 1: Запуск всех компонентов одной командой (рекомендуется)

```bash
python run.py
```

Эта команда запустит:
- 🤖 **Telegram Bot** (aiogram)
- 🌐 **API Server** (FastAPI на порту 8000)
- 📱 **Mini App** (на порту 8001)

Вы увидите:
```
==================================================
🍎 Запуск приложения Грядка
==================================================
🤖 Запуск Telegram бота...
🌐 Запуск API сервера...
📱 Запуск Mini App...

✅ Все компоненты запущены!

📝 Доступные эндпоинты:
   - API: http://localhost:8000
   - Mini App: http://localhost:8001
   - API Docs: http://localhost:8000/docs

⏸  Нажмите Ctrl+C для остановки
```

### Вариант 2: Раздельный запуск компонентов

Откройте **3 терминала** и в каждом активируйте виртуальное окружение:

**Терминал 1 - Telegram Bot:**
```bash
source venv/bin/activate  # или venv\Scripts\activate на Windows
python run_bot.py
```

**Терминал 2 - API Server:**
```bash
source venv/bin/activate
python run_api.py
```

**Терминал 3 - Mini App:**
```bash
source venv/bin/activate
python run_miniapp.py
```

---

## 🌐 Доступные URL

После запуска доступны:

| Сервис | URL | Описание |
|--------|-----|----------|
| **API Server** | http://localhost:8000 | REST API бэкенд |
| **API Docs** | http://localhost:8000/docs | Swagger документация |
| **Mini App** | http://localhost:8001 | Telegram Mini App (веб-интерфейс) |
| **Mini App клиентская часть** | http://localhost:8001/app | Главная страница магазина |
| **Админ-панель** | http://localhost:8001/app/admin | Панель администратора |
| **Telegram Bot** | В Telegram | Найдите вашего бота по username |

---

## 🧪 Тестирование

### 1. Проверка API

Откройте в браузере: http://localhost:8000/docs

Здесь вы увидите все доступные эндпоинты и сможете их протестировать.

**Основные эндпоинты:**
- `GET /api/products/` — список товаров
- `GET /api/products/categories` — список категорий
- `GET /api/cart/{telegram_id}` — корзина пользователя
- `GET /api/orders/{telegram_id}` — заказы пользователя
- `POST /api/admin/products` — создание товара (только админ)

### 2. Проверка Mini App

Откройте в браузере: http://localhost:8001/app

Вы увидите главную страницу магазина с:
- Шапкой и корзиной
- Поиском
- Баннерами акций
- Категориями
- Товарами

### 3. Проверка Админ-панели

Откройте в браузере: http://localhost:8001/app/admin

**Важно:** Для доступа нужен ваш `telegram_id`, который указан в `.env` как `ADMIN_ID`

В админке доступны:
- Статистика (заказы, выручка)
- Управление товарами и категориями
- Просмотр заказов
- Настройки магазина

### 4. Проверка Telegram Bot

1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start`
3. Вы должны увидеть приветственное сообщение
4. Нажмите кнопку "Открыть магазин" для запуска Mini App

---

## 📝 Тестовые данные

После инициализации БД создаются:

**Категории:**
- Все
- Ягоды
- Цитрусовые
- Яблоки
- Овощи

**Товары:**
- Клубника (450 ₽/кг) - бейдж "Хит"
- Апельсины (120 ₽/кг, старая цена 150 ₽) - бейдж "Акция"
- Яблоки Гала (85 ₽/кг)
- Помидоры (180 ₽/кг) - бейдж "Советую"

**Настройки:**
- Минимальная сумма заказа: 500 ₽
- Бесплатная доставка от: 2000 ₽
- Стоимость доставки: 200 ₽

**Промокод:**
- `WELCOME` - скидка 10% на заказ от 1000 ₽

**Интервалы доставки:**
- Утренняя доставка (10:00-15:00) - доступна для выбора с 00:00 до 12:00
- Вечерняя доставка (15:00-22:00) - доступна для выбора с 07:00 до 12:00

---

## 🔄 Перезапуск с чистой БД

Если нужно пересоздать базу данных с нуля:

```bash
# Остановите все процессы (Ctrl+C)

# Удалите базу данных
rm gryadka.db

# Инициализируйте заново
python init_db.py

# Запустите снова
python run.py
```

---

## 🛑 Остановка приложения

### Если запущено через `run.py`:
Нажмите **Ctrl+C** в терминале

### Если запущено раздельно:
Нажмите **Ctrl+C** в каждом из 3-х терминалов

---

## ⚠️ Решение проблем

### Ошибка: "No module named 'shared'"

**Причина:** Python не видит модули проекта

**Решение:**
```bash
# Убедитесь, что запускаете из корня проекта
cd /path/to/Gryadka

# И что виртуальное окружение активировано
source venv/bin/activate
```

### Ошибка: "BOT_TOKEN not found"

**Причина:** Не создан или неправильно заполнен `.env` файл

**Решение:**
1. Проверьте, что файл `.env` существует в корне проекта
2. Проверьте, что `BOT_TOKEN` правильно заполнен (без кавычек)
3. Перезапустите приложение

### Ошибка: "Address already in use"

**Причина:** Порт 8000 или 8001 уже занят

**Решение:**
```bash
# Найдите процесс на порту
lsof -i :8000
lsof -i :8001

# Убейте процесс (замените PID на реальный)
kill -9 PID
```

Или измените порты в `.env`:
```env
API_PORT=8002
```

### База данных не создается

**Причина:** Нет прав на запись в директорию

**Решение:**
```bash
# Проверьте права
ls -la gryadka.db

# Дайте права на запись
chmod 666 gryadka.db
```

### Ошибка при установке зависимостей

**Причина:** Старая версия pip

**Решение:**
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

---

## 📦 Структура после запуска

```
Gryadka/
├── gryadka.db              # База данных SQLite (создается автоматически)
├── venv/                   # Виртуальное окружение
├── .env                    # Ваши настройки (НЕ коммитить в Git!)
├── mini_app/static/uploads/ # Загруженные изображения товаров
└── __pycache__/            # Кэш Python
```

---

## 🔐 Безопасность (для разработки)

При локальной разработке:
- ✅ Используйте `.env` для секретов
- ✅ `.env` уже в `.gitignore`
- ✅ `SECRET_KEY` можно оставить простым
- ⚠️ Не используйте продакшн токены

---

## 🎯 Следующие шаги

После успешного запуска:

1. **Протестируйте функционал:**
   - Добавьте товар в корзину
   - Оформите тестовый заказ
   - Проверьте админ-панель

2. **Настройте под себя:**
   - Добавьте свои категории через админку
   - Загрузите реальные товары
   - Настройте интервалы доставки
   - Измените контактные данные

3. **Подготовка к деплою:**
   - Прочитайте `DEPLOYMENT.md`
   - Настройте продакшн базу данных (PostgreSQL)
   - Получите домен для Mini App

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в терминале
2. Откройте API Docs: http://localhost:8000/docs
3. Проверьте, что все зависимости установлены
4. Убедитесь, что `.env` файл правильно заполнен

---

**Готово! 🎉**

Приложение запущено и готово к разработке!
